Step,Action,Command,Purpose
1,Merge on GitHub,Click the Green Button,
Saves Jules's code into the remote master.


2,Update Local,     git pull origin master
                    git pull origin webhost


                    git log --oneline -5

                    "see the changes"


Downloads the merged code to your computer.

to see the branches including what was pulled
git branch -a

3,Cleanup Server,   git push origin --delete 

"Deletes the ""ghost"" branch from GitHub."

4,Refresh List,      git fetch --prune

Clears the deleted branch from your terminal view.



# 3. Enter the website directory (if you aren't already there)
cd apps/website

# 4. Sync your libraries (Essential if Jules added new tools)
npm install --legacy-peer-deps

# 5. Start the Web App (Backend) - MANDATORY for live data
# cd C:\Users\hnsil\Documents\GFC\cursor files\GFC-System\GFC-Studio V2\apps\webapp
# Open a NEW terminal for this
cd apps/webapp/GFC.BlazorServer
dotnet run

# 6. Start (or restart) the Website (Frontend)
# Open a SECOND terminal for this
# cd C:\Users\hnsil\Documents\GFC\cursor files\GFC-System\GFC-Studio V2\apps\website
cd apps/website
npm run dev


