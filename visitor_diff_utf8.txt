diff --git a/apps/webapp/GFC.BlazorServer/Components/Pages/KeyCards.razor b/apps/webapp/GFC.BlazorServer/Components/Pages/KeyCards.razor
index 3028cb11..36111e2b 100644
--- a/apps/webapp/GFC.BlazorServer/Components/Pages/KeyCards.razor
+++ b/apps/webapp/GFC.BlazorServer/Components/Pages/KeyCards.razor
@@ -341,7 +341,7 @@
         <!-- Mobile Actions Row (Wrapped) -->
         <div class="header-actions d-flex flex-wrap align-items-center gap-2 w-100 w-lg-auto ms-lg-auto mt-2 mt-lg-0 justify-content-start justify-content-lg-end">
             @* Grace Period Compact Badge *@
-            @if (_graceEndDate.HasValue)
+            @if (_graceEndDate.HasValue && _activeTab != "temporary")
             {
                 var daysRemaining = (_graceEndDate.Value - DateTime.Today).Days;
                 
@@ -373,19 +373,24 @@
                 </div>
             }
             
-            @if (_activeTab != "assignments" && _activeTab != "pending")
-            {
-                <button class="btn btn-outline-secondary btn-modern flex-grow-1 flex-lg-grow-0" @onclick='() => SetActiveTab("assignments")'>
-                    <i class="bi bi-arrow-left"></i>
-                    <span class="d-none d-lg-inline">Back to Keycards</span>
-                </button>
-            }
+            <button class="btn @(_activeTab == "assignments" || _activeTab == "pending" ? "btn-primary text-white" : "btn-outline-secondary") btn-modern flex-grow-1 flex-lg-grow-0" 
+                    @onclick='() => SetActiveTab("assignments")' title="Member Key Cards">
+                <i class="bi bi-people-fill"></i>
+                <span class="d-none d-lg-inline ps-2">Member Cards</span>
+            </button>
             
-            <button class="btn @(_activeTab == "activity" ? "btn-secondary text-white" : "btn-outline-secondary") btn-modern flex-grow-1 flex-lg-grow-0" 
+            <button class="btn @(_activeTab == "temporary" ? "btn-visitor-active text-white" : "btn-outline-secondary") btn-modern flex-grow-1 flex-lg-grow-0" 
+                    @onclick='() => SetActiveTab("temporary")' title="Visitor & Guest Cards">
+                <i class="bi bi-person-badge"></i>
+                <span class="d-none d-lg-inline ps-2">Visitor Cards</span>
+            </button>
+            
+            <button class="btn @(_activeTab == "activity" ? "btn-activity-active text-white" : "btn-outline-secondary") btn-modern flex-grow-1 flex-lg-grow-0" 
                     @onclick='() => SetActiveTab("activity")' title="Door Activity">
                 <i class="bi bi-activity"></i>
                 <span class="d-none d-lg-inline ps-2">Activity Log</span>
             </button>
+            
             <button class="btn btn-outline-primary btn-modern flex-grow-1 flex-lg-grow-0" @onclick="OpenIdentifyCardModal" title="Identify Card">
                 <i class="bi bi-search"></i>
                 <span class="d-none d-lg-inline ps-2">Identify Key Card</span>
@@ -395,8 +400,8 @@
                 <button class="btn btn-primary btn-modern flex-grow-1 flex-lg-grow-0" @onclick="OpenAssignCardModal">
                     <div class="btn-shine"></div>
                     <i class="bi bi-plus-lg"></i>
-                    <span class="d-none d-lg-inline ps-2">Assign Key Card</span>
-                    <span class="d-lg-none">New</span>
+                    <span class="d-none d-lg-inline ps-2">@(_activeTab == "temporary" ? "Assign Visitor Key Card" : "Assign Key Card")</span>
+                    <span class="d-lg-none">@(_activeTab == "temporary" ? "Visitor" : "New")</span>
                 </button>
             }
         </div>
@@ -429,7 +434,7 @@
     </div>
 }
 
-@if (_countPendingApproval > 0 || !_controllerOnline)
+@if ((_countPendingApproval > 0 || !_controllerOnline) && _activeTab != "temporary")
 {
     <div class="status-alerts-row d-flex flex-column flex-lg-row gap-2 mb-2 mx-4 mt-2">
         @if (!_controllerOnline)
@@ -738,6 +743,13 @@
         }
     </div>
 
+    <div class="tab-pane fade-in" style="@(_activeTab == "temporary" ? "" : "display:none")">
+        @if (_temporaryTabLoaded)
+        {
+            <TemporaryCardsTab @ref="_temporaryTab" />
+        }
+    </div>
+
     <div class="tab-pane fade-in" style="@(_activeTab == "access" ? "" : "display:none")">
         @if (_accessTabLoaded)
         {
@@ -767,6 +779,11 @@
     OnClose="CloseAssignCardModal"
     OnCompleted="HandleAssignmentCompleted" />
 
+<AssignVisitorKeyCardModal
+    IsVisible="_showVisitorAssignModal"
+    OnClose="() => _showVisitorAssignModal = false"
+    OnCompleted="HandleVisitorAssignmentCompleted" />
+
 <!-- Identify Card Modal -->
 @if (_showIdentifyModal)
 {
@@ -1157,6 +1174,7 @@
     private bool _activityTabLoaded = false;
     private bool _accessTabLoaded = false;
     private bool _pendingTabLoaded = false;
+    private bool _temporaryTabLoaded = false;
 
     private void SetActiveTab(string tab)
     {
@@ -1165,8 +1183,7 @@
         if (tab == "activity") _activityTabLoaded = true;
         if (tab == "access") _accessTabLoaded = true;
         if (tab == "pending") _pendingTabLoaded = true;
-
-
+        if (tab == "temporary") _temporaryTabLoaded = true;
     }
     
     // Card Filter State (shared with CardAssignmentsTab)
@@ -1183,6 +1200,8 @@
     
     private GFC.BlazorServer.Components.Pages.KeyCardTabs.MembersNeedingCardsTab? _needingCardsTab;
     private GFC.BlazorServer.Components.Pages.KeyCardTabs.CardAssignmentsTab? _assignmentsTab;
+    private GFC.BlazorServer.Components.Pages.KeyCardTabs.TemporaryCardsTab? _temporaryTab;
+    private bool _showVisitorAssignModal = false;
 
     // Identify Card Logic
     private bool _showIdentifyModal = false;
@@ -1384,12 +1403,29 @@
 
     private async Task OpenAssignCardModal()
     {
-        _isReplacementMode = false;
-        _selectedMemberIdForModal = 0;
-        _showAssignModal = true;
+        if (_activeTab == "temporary")
+        {
+            _showVisitorAssignModal = true;
+        }
+        else
+        {
+            _isReplacementMode = false;
+            _selectedMemberIdForModal = 0;
+            _showAssignModal = true;
+        }
         await Task.CompletedTask;
     }
 
+    private async Task HandleVisitorAssignmentCompleted()
+    {
+        _showVisitorAssignModal = false;
+        _operationSuccess = "Visitor card registered successfully.";
+        if (_temporaryTab != null)
+        {
+            await _temporaryTab.RefreshAsync();
+        }
+    }
+
     private void CloseAssignCardModal()
     {
         _showAssignModal = false;
