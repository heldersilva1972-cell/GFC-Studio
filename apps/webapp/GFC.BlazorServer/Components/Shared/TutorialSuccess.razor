@inject TutorialService TutorialService
@inject IJSRuntime JSRuntime

@if (Show)
{
    <div class="tutorial-success-overlay active">
        <canvas id="confetti-canvas"></canvas>
        
        <div class="success-card fade-in">
            <div class="success-icon-wrapper">
                <div class="success-icon">
                    <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                        <circle class="checkmark-circle" cx="26" cy="26" r="25" fill="none"/>
                        <path class="checkmark-check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
                    </svg>
                </div>
            </div>

            <h2 class="success-title">🎉 Tutorial Complete!</h2>
            <p class="success-message">
                You've successfully mastered the member onboarding process!
            </p>

            <div class="success-summary">
                <div class="summary-item">
                    <i class="bi bi-person-check-fill"></i>
                    <span>Added a new member</span>
                </div>
                <div class="summary-item">
                    <i class="bi bi-cash-coin"></i>
                    <span>Recorded dues payment</span>
                </div>
                <div class="summary-item">
                    <i class="bi bi-credit-card-fill"></i>
                    <span>Assigned & activated access card</span>
                </div>
            </div>

            <button class="btn btn-primary btn-lg btn-finish" @onclick="HandleFinish">
                <i class="bi bi-check-circle-fill me-2"></i>
                Finish Tutorial
            </button>
        </div>
    </div>
}

<style>
    .tutorial-success-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: linear-gradient(135deg, rgba(99, 102, 241, 0.95) 0%, rgba(139, 92, 246, 0.95) 100%);
        z-index: 10001;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        transition: opacity 0.5s ease;
    }

    .tutorial-success-overlay.active {
        opacity: 1;
    }

    #confetti-canvas {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
    }

    .success-card {
        background: white;
        border-radius: 24px;
        padding: 48px 40px;
        max-width: 500px;
        width: 90vw;
        text-align: center;
        box-shadow: 0 30px 90px rgba(0, 0, 0, 0.3);
        position: relative;
        z-index: 1;
    }

    .success-card.fade-in {
        animation: successFadeIn 0.6s cubic-bezier(0.4, 0.0, 0.2, 1) 0.3s both;
    }

    @@keyframes successFadeIn {
        from {
            opacity: 0;
            transform: scale(0.8) translateY(40px);
        }
        to {
            opacity: 1;
            transform: scale(1) translateY(0);
        }
    }

    .success-icon-wrapper {
        margin-bottom: 24px;
    }

    .success-icon {
        width: 100px;
        height: 100px;
        margin: 0 auto;
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 10px 30px rgba(16, 185, 129, 0.3);
        animation: iconPop 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) 0.5s both;
    }

    @@keyframes iconPop {
        0% {
            transform: scale(0);
        }
        50% {
            transform: scale(1.1);
        }
        100% {
            transform: scale(1);
        }
    }

    .checkmark {
        width: 56px;
        height: 56px;
    }

    .checkmark-circle {
        stroke: white;
        stroke-width: 3;
        stroke-dasharray: 166;
        stroke-dashoffset: 166;
        animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
    }

    .checkmark-check {
        stroke: white;
        stroke-width: 3;
        stroke-linecap: round;
        stroke-dasharray: 48;
        stroke-dashoffset: 48;
        animation: stroke 0.3s cubic-bezier(0.65, 0, 0.45, 1) 1.2s forwards;
    }

    @@keyframes stroke {
        100% {
            stroke-dashoffset: 0;
        }
    }

    .success-title {
        font-size: 2rem;
        font-weight: 800;
        color: #1e293b;
        margin: 0 0 16px 0;
        animation: slideUp 0.5s ease 0.7s both;
    }

    .success-message {
        font-size: 1.1rem;
        color: #64748b;
        margin: 0 0 32px 0;
        animation: slideUp 0.5s ease 0.8s both;
    }

    @@keyframes slideUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .success-summary {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        border-radius: 16px;
        padding: 24px;
        margin-bottom: 32px;
        animation: slideUp 0.5s ease 0.9s both;
    }

    .summary-item {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 12px 0;
        font-size: 1rem;
        color: #475569;
        font-weight: 600;
    }

    .summary-item:not(:last-child) {
        border-bottom: 1px solid #e2e8f0;
    }

    .summary-item i {
        font-size: 1.5rem;
        color: #10b981;
    }

    .btn-finish {
        border-radius: 14px;
        padding: 16px 40px;
        font-size: 1.1rem;
        font-weight: 700;
        box-shadow: 0 4px 16px rgba(99, 102, 241, 0.3);
        transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
        animation: slideUp 0.5s ease 1s both;
    }

    .btn-finish:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 24px rgba(99, 102, 241, 0.4);
    }
</style>

@code {
    [Parameter] public bool Show { get; set; }
    [Parameter] public EventCallback OnFinish { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (Show)
        {
            await StartConfettiAsync();
        }
    }

    private async Task StartConfettiAsync()
    {
        try
        {
            await JSRuntime.InvokeVoidAsync("eval", @"
                (function() {
                    const canvas = document.getElementById('confetti-canvas');
                    if (!canvas) return;
                    
                    const ctx = canvas.getContext('2d');
                    canvas.width = window.innerWidth;
                    canvas.height = window.innerHeight;

                    const confetti = [];
                    const colors = ['#6366f1', '#8b5cf6', '#ec4899', '#10b981', '#f59e0b'];
                    
                    for (let i = 0; i < 150; i++) {
                        confetti.push({
                            x: Math.random() * canvas.width,
                            y: Math.random() * canvas.height - canvas.height,
                            r: Math.random() * 6 + 4,
                            d: Math.random() * 150 + 10,
                            color: colors[Math.floor(Math.random() * colors.length)],
                            tilt: Math.random() * 10 - 10,
                            tiltAngleIncremental: Math.random() * 0.07 + 0.05,
                            tiltAngle: 0
                        });
                    }

                    function draw() {
                        ctx.clearRect(0, 0, canvas.width, canvas.height);

                        confetti.forEach((c, i) => {
                            ctx.beginPath();
                            ctx.lineWidth = c.r / 2;
                            ctx.strokeStyle = c.color;
                            ctx.moveTo(c.x + c.tilt + c.r, c.y);
                            ctx.lineTo(c.x + c.tilt, c.y + c.tilt + c.r);
                            ctx.stroke();

                            c.tiltAngle += c.tiltAngleIncremental;
                            c.y += (Math.cos(c.d) + 3 + c.r / 2) / 2;
                            c.tilt = Math.sin(c.tiltAngle - i / 3) * 15;

                            if (c.y > canvas.height) {
                                confetti[i] = {
                                    x: Math.random() * canvas.width,
                                    y: -20,
                                    r: c.r,
                                    d: c.d,
                                    color: c.color,
                                    tilt: c.tilt,
                                    tiltAngleIncremental: c.tiltAngleIncremental,
                                    tiltAngle: c.tiltAngle
                                };
                            }
                        });

                        requestAnimationFrame(draw);
                    }

                    draw();
                })();
            ");
        }
        catch (Exception)
        {
            // Silently fail if canvas not available
        }
    }

    private async Task HandleFinish()
    {
        if (OnFinish.HasDelegate)
        {
            await OnFinish.InvokeAsync();
        }
        else
        {
            await TutorialService.CompleteTutorialAsync();
        }
    }
}
