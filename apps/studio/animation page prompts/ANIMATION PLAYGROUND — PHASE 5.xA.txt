# ANIMATION PLAYGROUND — PHASE 5.xA
# PURPOSE: Add NEW advanced animations (3 total) *without removing* any existing Phase 5.1–5.10 animations.
# REQUIREMENTS:
# - Keep all existing animations fully working.
# - Add 3 brand-new advanced animations under the new category “Advanced Animations”.
# - Update UI so advanced animations appear in a dedicated section with preview thumbnails.
# - Do NOT break Phase 1–5.10 code. Do NOT rename or relocate existing animation files.
# - Ensure all animations render centered in viewport.
# - Modernized layout from Phase 4A must remain intact.

PHASE TASKS:
1. Create a new folder: app/animations/advanced/
2. Add 3 new advanced animations:
   A) "Galaxy Swirl" — particles orbiting in a 3D swirl using Three.js
   B) "Liquid Morph" — fluid SVG blob morphing with color cycling
   C) "Matrix Flow" — cascading digital rain effect with velocity variance

3. Each animation must be:
   - self-contained React client component
   - export default function
   - animation starts automatically when user enters its page
   - full-screen canvas or container
   - centered visually

4. Update animations index:
   File: app/animations/page.tsx  
   - Add new section titled “Advanced Animations (Phase 5.xA)”
   - Add new cards for each advanced animation
   - Use the same card style as existing animations

5. Update router pages:
   - Create pages:
       app/animations/advanced/galaxy-swarm/page.tsx
       app/animations/advanced/liquid-morph/page.tsx
       app/animations/advanced/matrix-flow/page.tsx

6. UI Updates:
   - In app/page.tsx ensure animation previews sit centered.
   - Maintain dark theme compatibility.
   - Maintain modern neon/tech aesthetic introduced earlier.

7. Provide complete code for:
   - Each new animation
   - Updated animations list
   - Any CSS additions
   - Any layout upgrades required to center animations

8. Preserve functionality:
   - All existing Phase 5.1–5.10 animations must remain untouched & functional.
   - Layout improvements must not break existing pages.

OUTPUT FORMAT REQUIRED:
- Provide updated and new file contents in full blocks.
- Each file in its own block.
- No partial code.
- No ellipses.
- No summaries.
- Cursor must apply changes exactly as given.

END OF PHASE 5.xA
