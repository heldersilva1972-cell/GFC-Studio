# PROJECT: Animation Playground – Phase 4B
# PURPOSE: Add the "Animation Preview Panel + Code Copy Window" feature.
# RULES: Standalone project, Next.js/React/TS/Tailwind, App Router, component-based structure, Framer Motion. Do not remove existing animations. Only add files or extend features. Maintain revision header in every file.

PHASE 4B TASKS:
1) Create a right-side “Preview Panel” that displays the selected animation.
2) Create a bottom collapsible “Code Window” that shows the complete TSX code for the selected animation.
3) Add a “Copy Code” button (clipboard API).
4) Integrate these panels into the existing layout created in Phase 4.
5) Ensure zero breaking changes to Phase 1–4.
6) Panels must animate in/out using Framer Motion.
7) Add a global component: /components/AnimationPreview.tsx
8) Add a global component: /components/CodeViewer.tsx
9) Update main page (/app/page.tsx) to support:
   - SelectedAnimation state
   - PreviewPanel + CodeWindow placement
10) The CodeViewer must auto-load the matching animation component source using static imports only (no dynamic fs).

FILES TO CREATE OR MODIFY:
- /components/AnimationPreview.tsx
- /components/CodeViewer.tsx
- /components/AnimationList.tsx (append selection callback only)
- /app/page.tsx (inject layout + states)
- /styles/globals.css (if needed for scrollbars)

REQUIREMENTS:
- Preview panel: right side, fixed width ~40%, scrollable, white bg, rounded-xl, shadow-lg.
- Code viewer: collapsible, full-width bottom panel, <pre><code> styling with Tailwind “prose” class.
- Add motion.div transitions (slide-in from right for Preview, slide-up for Code).
- Provide placeholder text when no animation selected.
- Add "Copy Code" button: small, top-right, blue.

DO:
- Maintain existing animations untouched.
- Keep UI modern & minimal.
- Use Tailwind and Framer Motion only.
- Use TypeScript strict mode.

DO NOT:
- Alter existing animation code.
- Add new animations (Phase 5.x only).
- Introduce server components.

WHEN COMPLETE:
Reply “Phase 4B applied”.
