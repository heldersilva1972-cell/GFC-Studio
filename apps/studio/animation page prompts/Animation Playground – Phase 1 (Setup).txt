# Animation Playground – Phase 1 (Setup)
# Rev: AP-PH1-R1

GOAL: Scaffold standalone Animation Playground project. Next.js + TS + Tailwind; clean structure; isolated from GFC Web App. Include local preview window for animations + one-click copy-code panel.

TASKS:
1) Create new Next.js 15 App Router project: /animation-playground
   - TS, Tailwind, Shadcn/UI, Framer Motion installed.
   - Folder structure:
     /app
       /animations
       /preview
       /components
       /ui
     /lib
     /data
2) Build layout:
   - Left sidebar: animation list (placeholder items).
   - Main area: preview window w/ responsive frame + “Copy Code” button.
   - Right panel: metadata (name, category, difficulty).
3) Build global components:
   - <AnimationPreview> loads selected animation via dynamic import.
   - <CodePanel> shows full source (TSX/CSS) w/ “Copy” button.
   - <AnimationCard> in list.
4) Add demo placeholder animation: “PulseBox”.
   - Framer Motion based.
   - Show code in CodePanel.
5) Add state mgmt (useState) for selected animation.
6) Add basic routing:
   - /animations (list)
   - /animations/[id] (preview)
7) Add theme: modern dark/light; Tailwind classes.
8) Add initial metadata file: /data/animations.ts (id, name, category, file path).
9) Ensure all code self-contained, no external API, runs locally.
10) After generating code: run “npm install” instructions + dev server tips.

OUTPUT:
- Full project code.
- All files and folders.
- No placeholders missing.
- Ensure animations load dynamically.
- Ask before moving to Phase 2.
